<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.trabajo.jorch.animacionesbasicas.MainActivity">

    <TextView
        android:text="ANIMACIONES"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:textAppearance="@android:style/TextAppearance.Material.Large"
        android:textAlignment="center"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:textStyle="normal|bold" />

    <Button
        android:text="ROTATE"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_alignParentStart="true"
        android:layout_marginTop="22dp"
        android:id="@+id/buttonRotate" />

    <Button
        android:text="TRANSLATE"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/buttonAlpha"
        android:layout_toEndOf="@+id/buttonAlpha"
        android:id="@+id/buttonTranslate" />

    <Button
        android:text="ALPHA"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonAlpha"
        android:layout_alignBaseline="@+id/buttonRotate"
        android:layout_alignBottom="@+id/buttonRotate"
        android:layout_toEndOf="@+id/buttonRotate" />

    <Button
        android:text="SCALE"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonScale"
        android:layout_alignBaseline="@+id/buttonTranslate"
        android:layout_alignBottom="@+id/buttonTranslate"
        android:layout_toEndOf="@+id/buttonTranslate" />

    <ImageView
        app:srcCompat="@drawable/mundo"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/imageViewMundo"
        android:layout_width="250sp"
        android:layout_height="200sp" />
</RelativeLayout>
